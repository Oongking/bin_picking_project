
import numpy as np


# axis_xyz = np.eye(3)

# for axis in axis_xyz:
#     print(f"axis: {axis}")

x = np.array([1,0,0])
y = np.array([0,1,0])
z = np.array([0.,0.,0.])

# print(np.absolute(np.cross(x,x)))
# np.any(my_array[:, 0] == value)

print(np.all(z[:] == 0))